<h2>All Students:</h2>

<ul>
  <% @students.each do |student| %>
    <li><%= link_to student.name, student_path(student) %></li>
  <% end %>
</ul>

<br>
<br>

Make new students:
<%= form_tag(new_student_path, method: :get) do %>
  <%= label_tag "How many teachers?" %>
  <%= number_field_tag :how_many %>
  <%= submit_tag "Make new student" %>
<% end %>
